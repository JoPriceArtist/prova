const { getResultsForProject, getProjectsForCourse } = require('../dont-implement/projectManager')

/// <reference path="../types/types.js" />

/**
 * Returns a collection of results for all projects for the defined student and year.
 * @param {number} studentId - eg 27
 * @param {string} courseId - WDD03
 * @param {number} year - e.g 1975
 * @returns {CourseTranscript} 
 */
const getCourseTranscript = (studentId, courseId, year) => {
    // use project manager to get all the projects for the course
   

    //use project manager to get results for all these projects for the passed in student


    // return a well formed course transcript
}


module.exports = getCourseTranscript;
